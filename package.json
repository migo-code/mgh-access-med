{
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "tsc --noEmit"
  },
  "private": "true",
  "packageManager": "npm@10.9.2",
  "workspaces":[
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
